# Your actual router configs go in this directory
# These files are gitignored and NOT committed to the repo
#
# Copy the example files here and fill in your real values:
#   cp config/parental_profiles.example config/local/parental_profiles
#   cp config/parental_websites.example config/local/parental_websites
#   cp config/mac_allowlist.example config/local/mac_allowlist
#   cp config/parental_blocklists.example config/local/parental_blocklists
#
# You can also add:
#   config/local/crontab       - your custom cron schedules
#   config/local/tickets.html  - web UI with your profile names
#
# Then run: ./install.sh 192.168.1.1
# The installer uses these files if they exist, otherwise creates templates on the router.